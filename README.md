# WebSocket Чат с JWT Аутентификацией

Простое приложение для обмена сообщениями в реальном времени, построенное на Go (фреймворк Gin), WebSocket и JWT аутентификации.

## Возможности

- 🔐 JWT-аутентификация (регистрация/вход)
- 💬 Обмен сообщениями в реальном времени через WebSocket
- 👥 Многопользовательский чат
- 🎨 Современный адаптивный интерфейс
- 🔒 Хеширование паролей с помощью bcrypt

## Установка

1. Перейдите в директорию проекта:
```bash
cd simple-chat
```

2. Установите зависимости:
```bash
go mod download
```

## Запуск приложения

1. **Запустите PostgreSQL**:
   ```bash
   docker-compose up -d
   ```
3. **Запустите сервер**:
   ```bash
   go run .
   ```
   
   Приложение автоматически:
   - Подключится к PostgreSQL
   - Создаст таблицу `users`, если она не существует
   - Выполнит миграции базы данных

4. **Откройте браузер** и перейдите по адресу:
   ```
   http://localhost:8080
   ```

5. **Зарегистрируйте новый аккаунт** или войдите с существующими учетными данными.

## API Endpoints

### Аутентификация

- `POST /api/register` - Регистрация нового пользователя
  ```json
  {
    "username": "user123",
    "password": "password123"
  }
  ```

- `POST /api/login` - Вход с учетными данными
  ```json
  {
    "username": "user123",
    "password": "password123"
  }
  ```

### Защищенные Endpoints

- `GET /api/me` - Получить информацию о текущем пользователе (требуется Bearer token)
- `GET /api/ws?token=<jwt_token>` - WebSocket подключение для чата

## Формат сообщений WebSocket

### От клиента к серверу
```json
{
  "type": "message",
  "content": "Hello, world!"
}
```

### От сервера к клиенту
```json
{
  "type": "message",
  "username": "user123",
  "content": "Hello, world!",
  "time": "2024-01-01T12:00:00Z"
}
```

Системные сообщения (вход/выход пользователя):
```json
{
  "type": "system",
  "username": "user123",
  "content": "user123 joined the chat",
  "time": "2024-01-01T12:00:00Z"
}
```

## Структура проекта

```
simple-chat/
├── main.go              # Точка входа сервера и маршрутизация
├── config.go            # Управление конфигурацией
├── config.yaml.example  # Пример файла конфигурации
├── docker-compose.yml   # Конфигурация Docker Compose для PostgreSQL
├── database.go          # Подключение к базе данных и модели
├── auth.go              # Обработчики аутентификации и JWT сервис
├── websocket.go         # Управление WebSocket hub и клиентами
├── utils.go             # Вспомогательные функции
├── go.mod               # Зависимости Go модуля
├── README.md            # Этот файл
├── .gitignore           # Правила Git ignore
├── templates/           # HTML шаблоны
│   ├── index.html
│   ├── login.html
│   └── register.html
└── static/              # Статические ресурсы
    ├── style.css
    ├── chat.js
    ├── login.js
    └── register.js
```

## Схема базы данных

Приложение автоматически создает следующую таблицу:

### users
- `id` (UUID, Первичный ключ) - Автоматически генерируемый UUID
- `username` (VARCHAR, Уникальный, Not Null) - Имя пользователя
- `password` (VARCHAR, Not Null) - Хешированный пароль Bcrypt

Таблицы автоматически создаются с помощью функции auto-migration GORM при запуске приложения.
